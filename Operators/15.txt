#include<stdio.h>
int main()
{
    int num1, num2, square1, square2, sum;
    printf("Enter any two numbers: ");
    scanf("%d%d", &num1, &num2);
    square1 = num1*num1;
    square2 = num2*num2;
    sum = squarer1 + squarer2;
    printf("Sum of their square = %d", sum);
    return 0;
}